# The Windows residue lane (test-residue-windows) runs secmem-crypto's
# out-of-process key residue scan on windows-latest. The victim raises its own
# working-set minimum before it locks anything, and a parent may always read
# and suspend the child it started, so NOTHING may skip: a skipped scenario is
# a row of the protection table that was not measured.
